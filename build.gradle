/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'peter' at '3/12/14 7:43 PM' with Gradle 1.11
 *
 * This generated file contains a commented-out sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at http://gradle.org/docs/1.11/userguide/tutorial_java_projects.html
 */

// def proc = 'for t in `git tag -l`; do git cat-file tag $t; done'
// proc.execute()

task readTags(type:Exec) {
 
 commandLine 'git', 'tag', '-l'   
 

  //extension method stopTomcat.output() can be used to obtain the output:
  ext.output = {
    return standardOutput.toString()
  }
}

task tagger(dependsOn:readTags) {
    println readTags.output
}
